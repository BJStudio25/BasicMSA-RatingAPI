spring:
  cloud: 
    config: 
      name: rating-service
      discovery:
        service-id: config
        enabled: true
      username: configUser
      password: configPassword

eureka:
  client:
    service-url:
      default-zone: http://discUser:discPassword@localhost:8082/eureka/